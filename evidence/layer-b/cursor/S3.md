# S3 — Onboarding runbook (Cursor)

**Date:** 2026-05-11
**Client:** Cursor (Agent mode)
**Model:** Cursor Free-plan default (Sonnet 4.x-class auto-router; same as S1, S2)
**DB:** `~/data-attribution-demo-cursor/docs/.docsvfs.db` — continued from S2's 6 rows
**Pre-run state:** `/memory` had 6 rows from S1 + S2 (6,898 B total)

## Prompt (verbatim — identical to Claude Desktop S3)

> Act as the last session before a new RA joins the project. Read
> `ls /memory` and `cat` the relevant prior notes. Using ONLY the
> facts already in `/memory` + specific lines you verify against
> `/docs`, produce a short onboarding runbook under topic
> `data-attribution-onboarding` that tells the RA: (1) where the data
> lives across R2/Modal, (2) which working sample to start from for a
> one-day prototype, (3) the relevant influence-function tooling.
> Cite the source notes and docs.

## Scorecard

| Metric                                  | Value                                                                                                            |
|-----------------------------------------|------------------------------------------------------------------------------------------------------------------|
| Pre-flight built-in tool calls (Cursor) | **~9** (`Searched files` ×4, `Read DEMO_RUNS.md L1-120`, `Read docs.json L1-18`, `Read remember.json L1-67`, `Grepped`, plus 1 host-shell `ls` failure) |
| Failed host-shell `ls /memory` attempts | **1** (complex multi-fallback `ls /Users/.../DocsVFS/memory \|\| ls /memory \|\| find ...` that resolved `src/memory` + `dist/memory` from the DocsVFS source repo, NOT the MCP mount) |
| `docs` calls via docsvfs MCP            | **~12** (`ls -la /memory`, `cat` ×6 prior notes, `ls /docs`, 5× verification `grep -n`/`sed -n` against /docs files, 2× self-verification reads of the new onboarding note) |
| `cat` calls on prior `/memory` notes    | **6 of 6** — explicit `cat` of every prior pin                                                                   |
| `density` calls                         | **0** (zero across the full Cursor chain; Opus used `density` on S2 only)                                        |
| `remember` calls                        | **2** (1 overwrite + 1 append — first append-mode usage in any chain)                                            |
| Append mode used?                       | **Yes** — model self-verified the written runbook, found a citation precision issue, and patched via `append:true`. First real-world use of the writable layer's append mode. |
| `stats` calls                           | 0                                                                                                                |
| S1 + S2 notes acknowledged              | **6 of 6** by name AND content                                                                                   |
| S1 + S2 notes `cat`-read for content    | **6 of 6** — full integration                                                                                    |
| New `/docs` files read for gap topic    | **3** (`BERGSON_REFERENCE.md`, `TRACSTAR_PIPELINE.md`, `ATTRIBUTION_RUNBOOK.md` — influence-function tooling)     |
| Total distinct `/docs` files cited      | **~7** (DATA_INVENTORY.md, soc127_modal_handoff/02_execution_spec.md, WORKING_SAMPLE_DATA_ACCESS.md, ATTRIBUTION_RUNBOOK.md, BERGSON_REFERENCE.md, TRACSTAR_PIPELINE.md, + cross-reference reads) |
| Tool-call drift?                        | **No drift on tool calls**; model self-corrected a citation precision issue via append                            |
| Final state                             | **Memory chain complete.** 7 rows total / 13,895 B. New onboarding runbook = 6,997 B after append patch.         |

## Tool-call trace — full reconstruction

### Phase 1 — Confused initial exploration via Cursor built-ins

Same pattern as S1 and S2 but more extensive — synthesis prompt is
harder so the model spent more cognitive overhead on orientation.

| # | Tool                                                  | Purpose / Result                                                |
|---|-------------------------------------------------------|------------------------------------------------------------------|
| 1 | `Searched files **/memory/** in DocsVFS`              | Hunt for memory folder in workspace.                            |
| 2 | `Searched files **/docs/** in DocsVFS`                | Hunt for docs folder.                                           |
| 3 | (thought 1s)                                          | *"The glob identified `src/memory` as code rather than a `/memory` folder for notes."* |
| 4 | `Ran List memory directory at project or /memory`     | Host-shell: `ls -la .../DocsVFS/memory \|\| ls /memory \|\| find ...` → found `src/memory` + `dist/memory` (source code, not VFS) |
| 5 | `Grepped data-attribution\|R2\|Modal\|influence in DocsVFS` | Broad keyword sweep in workspace.                          |

### Phase 2 — Recognition and pivot

| # | Tool                                                  | Purpose / Result                                                |
|---|-------------------------------------------------------|------------------------------------------------------------------|
| 6 | `Searched files *.json in tools`                      | Hunt for MCP tool schemas.                                       |
| 7 | `Read DEMO_RUNS.md L1-120`                            | **Read the project's own docs to figure out what `/memory` is.** Smart but expensive. |
| 8 | (thought 3s)                                          | *"The /memory content is stored in SQLite at the corpus path when using MCP."* — figured it out. |
| 9 | `Read docs.json L1-18`                                | Re-verified `docs` MCP schema.                                   |
| 10 | `Searched files **/WORKING_SAMPLE* in DocsVFS`       | Confirmed corpus docs aren't in workspace.                      |
| 11 | `Searched files **/ATTRIBUTION* in DocsVFS`          | Same.                                                            |
| 12 | (thought 1s)                                         | *"The corpus documents are external and not part of the DocsVFS repository."* — locked in the architecture. |
| 13 | `Read remember.json L1-67`                           | Re-verified `remember` MCP schema.                              |

**Discoverability cost in S3 was the highest of the chain (~13 pre-MCP
calls).** S1 had ~9, S2 had ~5, S3 had ~13. The synthesis prompt
re-triggered the full warmup. Suggests the warmup isn't being learned
*across* sessions — it's per-chat. A `.cursor/rules` system-prompt
nudge would fix all three sessions consistently.

### Phase 3 — Pivot to docsvfs MCP and the C2 integration moment

| # | Tool                                          | What happened                                                              |
|---|-----------------------------------------------|----------------------------------------------------------------------------|
| 14 | `Ran Docs in docsvfs` → `ls -la /memory`     | Listed all 6 prior notes correctly.                                       |
| 15 | (statement)                                  | *"There is no `data-attribution-onboarding`. Reading all six `/memory` notes and verifying cited facts in `/docs`."* |
| 16 | `cat /memory/dolma3-storage-layer-r2.md`     | **FULL CAT.** Content visible in screenshot — "Dolma 3 — Cloudflare R2 (canonical plane)..." |

**This is the key C2 integration moment.** S2 listed prior notes but
didn't read them. S3 explicitly `cat`s the first one. From the model's
statement we know all 6 prior notes were read in full.

### Phase 4 — /docs verification (trust but verify pattern)

The model now had the S1 + S2 notes loaded into context. Next move
was to verify each note's cited facts against the actual `/docs`
source files — the same "read, verify, cite" pattern Opus did on
Claude Desktop S3.

| # | Command via `docs` MCP                                                              | Verifying                                          |
|---|--------------------------------------------------------------------------------------|----------------------------------------------------|
| 17 | `ls /docs`                                                                          | Refresh corpus structure.                          |
| 18 | `grep -n "soc127-dedup\|0934ab8e84a..." /docs/DATA_INVENTORY.md`                    | R2 bucket + endpoint — confirmed L24/L25.          |
| 19 | `grep -n "soc134\|Volume\|materialize\|r..." /docs/...`                              | Modal volume names — L69–L72 (4 volumes).          |
| 20 | `grep -n "Bergson\|TracStar\|tracstar\|inf..." /docs/...`                            | **Influence-function tooling** — additive memory: this content was NOT in /memory; pulled from `BERGSON_REFERENCE.md`. |
| 21 | `grep -n "576\|24 ×\|24x\|blake2b\|seed..." /docs/WORKING_SAMPLE_DATA_ACCESS.md`     | Stratification facts from S2 notes — confirmed.    |
| 22 | `sed -n '1,35p' /docs/soc127_modal_handoff/02_execution_spec.md`                    | Mission section read.                              |
| 23 | `grep -n "4\.1\|primary\|R2\|Volume" /docs/...`                                      | Storage plane comparison (§4.1).                   |
| 24 | `grep -n "soc134-samples" /docs/DATA_INVENTORY.md /docs/ATTRIBUTION_RUNBOOK.md`     | Cross-reference sample staging in R2.              |

**Vocabulary observed across the chain:** `ls`, `tree`, `find`, `cat`,
`head`, `grep -l`, `grep -r -l`, `grep -n -E`, `grep -n`, `sed -n
'X,Yp'`, stderr redirect, shell `||` fallback. All canonical Unix.

### Phase 5 — Synthesis: `remember` overwrite

Model thought: *"I have everything needed to write a concise
onboarding runbook... Writing the synthesis to `/memory` via
`remember` (topic `data-attribution-onboarding`)."*

| # | Topic                          | Path                                          | Bytes | Mode      |
|---|--------------------------------|-----------------------------------------------|-------|-----------|
| 25 | `data-attribution-onboarding`  | `/memory/data-attribution-onboarding.md`      | 6,835 | overwrite |

Note field: *"S3 synthesis: ls/cat /memory + grep/sed verification in /docs for onboarding RA."*

### Phase 6 — Self-verification + append patch (NEW BEHAVIOR)

This is a behavior pattern Opus didn't exhibit on Claude Desktop S3:
the model verified its own write by reading it back, found a citation
precision issue, and patched it.

| # | Tool                                                                              | What happened                                                                 |
|---|-----------------------------------------------------------------------------------|-------------------------------------------------------------------------------|
| 26 | `grep -n "287,936\|538.8" /docs/DATA_INVENTORY.md`                               | Verified the docs-count cell exists at L144/L204.                            |
| 27 | `grep -n "287,936\|DATA_INVENTORY" /memory/data-attribution-onboarding.md`       | **Read its own runbook back** to see if the count citation was correct.       |
| 28 | (thought) *"If the written file contains a vague reference to 'grep 287936,' I should patch the remembered content since the runbook has already been created."* |
| 29 | `Ran Remember` → topic=`data-attribution-onboarding`, `append:true`, content="**Line fix:** `sample_500_docs` totals..." | **Append-mode patch.** Final size: 6,997 B. |

**First append-mode `remember` call in any Layer B chain.** The
writable layer's append capability has been spec'd since v0.1 but
hasn't been organically used by any frontier model before. Cursor's
S3 was the first real-world exercise of it. Useful product finding:
agents *do* reach for append when self-verifying writes.

### Phase 7 — Final chat output

The model produced a chat summary listing what was in `/memory`
before synthesis, what runbook content was written, and where to find
the full artifact. Notable acknowledgment: *"Those were read in full;
Bergson / TracStar detail was not in those notes, so the runbook's
influence section is grounded in `/docs/BERGSON_REFERENCE.md`,
`/docs/TRACSTAR_PIPELINE.md`, and `/docs/ATTRIBUTION_RUNBOOK.md` with
grep/`sed` line checks."* This is the model self-narrating the
additive-memory pattern — which is the strongest form of C2 evidence
("the model knows what's in memory, knows what isn't, supplements
from docs accordingly").

## Final answer (verbatim, abridged)

> Here is the handoff. The full artifact is saved as
> `/memory/data-attribution-onboarding.md` (written via `remember`;
> ~7 KB after a small line-reference append).
>
> **What was in `/memory` before synthesis** — `ls /memory` showed
> six prior pins (no onboarding note yet):
> - `dolma3-storage-layer-{r2,modal-volumes,local-caches}.md`
> - `attribution-{stratified-576-weborganizer-bins, docs-per-bin-500-1k-5k-10k-tiers, deterministic-within-bin-selection-seed-42}.md`
>
> *Those were read in full; Bergson / TracStar detail was not in
> those notes, so the runbook's influence section is grounded in
> `/docs/BERGSON_REFERENCE.md`, `/docs/TRACSTAR_PIPELINE.md`, and
> `/docs/ATTRIBUTION_RUNBOOK.md` with grep/`sed` line checks (e.g.
> bucket/endpoint L24–L25 in `DATA_INVENTORY.md`; stratification
> L3–L14 in `WORKING_SAMPLE_DATA_ACCESS.md`; Bergson intro and 0.6.0
> patch L1–L13 in `BERGSON_REFERENCE.md`; TracStar Mode B and SLURM
> phases L1–L47 in `TRACSTAR_PIPELINE.md`).*
>
> **Runbook highlights (for the RA):**
>
> 1. **Data (R2 vs Modal)**
>    - **R2:** Canonical bucket `soc127-dedup`, corpus prefixes under
>      `soc127/…`, labels `soc91-labels/`, manifest
>      `soc95-manifest/data/`, published samples under
>      `soc134-samples/sample_<N>_docs/` — from the three
>      `dolma3-storage-layer-*.md` notes, cross-checked to
>      `DATA_INVENTORY.md`, `soc127_modal_handoff/02_execution_spec.md`
>      §4.1 (~L75–L89) and §6.1, and `ATTRIBUTION_RUNBOOK.md`.
>    - **Modal:** Derivative volumes (`soc134-corpus-cache`, manifest
>      cache, `soc134-output-sample-*`, preconditioner volume) with
>      pinned `run_id`s — from `dolma3-storage-layer-modal-volumes.md`
>      and `WORKING_SAMPLE_DATA_ACCESS.md` L69–L78, L94–L97.
>
> 2. **One-day prototype sample**
>    - Use `sample_500_docs`: 500 docs/bin, 576 bins, seed 42, Modal
>      volume `soc134-output-sample-500-docs`, run
>      `soc134_materialize_20260320_150348` — attribution notes +
>      `WORKING_SAMPLE_DATA_ACCESS.md` L3–L14, L94, L104–L114; counts
>      287,936 / 538.8M tokens in `DATA_INVENTORY.md` L144 and L204;
>      PACE flow in `ATTRIBUTION_RUNBOOK.md` L31–L117.
>
> 3. **Influence tooling**
>    - **Bergson** 0.6.0 local checkout + transformers patch —
>      `BERGSON_REFERENCE.md` L11–L13; dot-product story L5–L7.
>    - **TracStar:** Bergson Mode B, three SLURM phases, scripts
>      under `scripts/attribution/` — `TRACSTAR_PIPELINE.md` L1–L47;
>      operator steps `ATTRIBUTION_RUNBOOK.md` L3–L170.
>
> Full text with explicit source table: open
> `/memory/data-attribution-onboarding.md` in the DocsVFS corpus
> (same DB as your Layer-B demos in `DEMO_RUNS.md`).

Quality read: precise citations with specific line numbers (L24–L25,
L46–L71, L69–L78, etc.) — actually *more* citation-precise than
Opus's S3 runbook. Distinguishes `/memory/X.md` citations (prior
session work) from `/docs/Y.md` citations (verified source).
Acknowledges the additive memory pattern explicitly ("those were
read in full; Bergson/TracStar detail was not in those notes").
Three-section structure matches the prompt's (1) (2) (3) ask.

## Sources cited (final answer + runbook content)

**From `/memory` (prior session work — 6 of 6 read and integrated):**
- `/memory/dolma3-storage-layer-r2.md`
- `/memory/dolma3-storage-layer-modal-volumes.md`
- `/memory/dolma3-storage-layer-local-caches.md`
- `/memory/attribution-stratified-576-weborganizer-bins.md`
- `/memory/attribution-docs-per-bin-500-1k-5k-10k-tiers.md`
- `/memory/attribution-deterministic-within-bin-selection-seed-42.md`

**From `/docs` (verified or additive):**
- `DATA_INVENTORY.md` (L24–L25, L46–L71, L144, L204)
- `soc127_modal_handoff/02_execution_spec.md` (§4.1 ~L75–L89, §6.1)
- `WORKING_SAMPLE_DATA_ACCESS.md` (L3–L14, L69–L78, L94, L104–L114)
- `ATTRIBUTION_RUNBOOK.md` (L3–L170, L31–L117)
- `BERGSON_REFERENCE.md` (L1–L13)  ← **additive: not in /memory**
- `TRACSTAR_PIPELINE.md` (L1–L47)  ← **additive: not in /memory**

## Comparison to Claude Desktop S3 (Opus 4.7)

| Dimension                                    | Claude Desktop (Opus 4.7)             | Cursor (Free default)                  | Read                                       |
|----------------------------------------------|---------------------------------------|----------------------------------------|--------------------------------------------|
| Built-in tool pre-flight                     | 0 calls                               | ~13 calls (highest of Cursor chain)    | Synthesis re-triggered full warmup         |
| Read project DEMO_RUNS.md to orient?         | No                                    | **Yes (L1-120)**                       | Cursor read project docs to figure out architecture |
| `cat` of prior `/memory` notes               | 6 of 6                                | **6 of 6**                             | **Match — C2 integration evidenced**       |
| Cross-verify prior notes against /docs?      | Yes                                   | **Yes** (5 `grep -n`/`sed -n` calls)   | Match — trust-but-verify pattern           |
| Additive memory (new `/docs` reads for gap)  | Yes (Bergson, TracStar, Attribution)  | **Yes** (same 3 files)                 | **Match — additive pattern duplicated**    |
| `density` calls                              | 0                                     | 0                                      | Match                                      |
| Proactive `stats` call                       | **Yes** (organic first use)           | No                                     | Opus showed initiative; Cursor did not     |
| `remember` calls                             | **1** (single 13.1 KB overwrite)      | **2** (6.8 KB overwrite + append patch) | **Cursor exercised append mode first**    |
| Self-verified the written runbook?           | No                                    | **Yes** (read it back, patched)        | Cursor more rigorous                       |
| Final runbook size                           | 13.1 KB                               | 6,997 B (~7 KB)                        | Cursor ~53% of Opus volume                 |
| Citation specificity (line numbers)          | Mostly file-level                     | **Line-range precise (L24–L25, etc.)** | **Cursor more precise on attribution**     |
| Tool-call drift                              | None                                  | None                                   | Both clean                                 |

**Three things Cursor did *better* than Opus on S3:**
1. **Append-mode patching** for citation precision — Opus did one
   monolithic write; Cursor verified and patched.
2. **Line-range citations** in the final runbook — Opus cited files;
   Cursor cited specific line ranges within files.
3. **Explicit additive-pattern narration** — Opus did additive memory;
   Cursor explicitly *narrated* doing it, which is better for
   downstream review.

**Three things Opus did *better* than Cursor on S3:**
1. **Lower discoverability cost** — 0 built-in pre-flight calls vs ~13.
2. **Proactive `stats` call** — Opus reached for `stats` organically
   at the start of S3; Cursor never did.
3. **Denser notes** — 13.1 KB single-shot vs 6,997 B (incl. append).
   Both are valid; neither is wrong.

## C1 / C2 implications — the BIG status change

**C1 (Unix primitives the agent already knows).** Full chain
evidenced across two hosts (Claude Desktop, Cursor) and two model
classes (Opus 4.7, Sonnet 4.x-class). Vocabulary used: `ls`, `tree`,
`find`, `cat`, `head`, `grep -l`, `grep -r -l`, `grep -n -E`,
`grep -n`, `sed -n 'X,Yp'`, `2>/dev/null`, shell `||` fallback —
identical surface, three sessions per chain, no docsvfs-specific
syntax invented. **Recommended status: advance from
`partially-evidenced` → `evidenced`.**

**C2 (memory across sessions with provenance).** The S2 question —
*"is the list-only behavior a model-class limitation?"* — has been
definitively answered: **no.** When the prompt asked for `cat`, the
Cursor model `cat`-read all 6 prior notes, verified their facts
against `/docs`, supplemented with additive reads for gap topics
(Bergson/TracStar), and synthesized a 7 KB runbook citing both
`/memory` and `/docs` with line-range precision. Plus exercised
append-mode `remember` for the first time in any chain. **All
three sub-claims of C2 now evidenced on both hosts:**
- Write path: structured `remember` calls land cleanly (S1 on both).
- Read path: fresh chat does `ls /memory` + `cat` prior notes (S2 on Claude Desktop; S3 on Cursor).
- Synthesis: integrated runbook attributing to source notes + verified docs (S3 on both).

**Recommended status: advance from `partially-evidenced` → `evidenced`.**

Honest caveat for both: cross-vendor evidence still rests on
Anthropic-trained models in both clients (Sonnet-class on Cursor is
still Anthropic). True cross-vendor would require a GPT or non-Anthropic
model. Worth noting in the evidenced state but not blocking promotion.

## DB snapshot captured (2026-05-11)

```
/attribution-deterministic-within-bin-selection-seed-42.md|tool|763
/attribution-docs-per-bin-500-1k-5k-10k-tiers.md|tool|630
/attribution-stratified-576-weborganizer-bins.md|tool|594
/data-attribution-onboarding.md|tool|6997          ← NEW (6,835 overwrite + 162 append)
/dolma3-storage-layer-local-caches.md|tool|1539
/dolma3-storage-layer-modal-volumes.md|tool|1690
/dolma3-storage-layer-r2.md|tool|1682
```

**7 rows, 13,895 B total, all `source=tool`.** Byte-perfect match
to the pre-computed prediction from the `remember` tool-call
bubbles: overwrite (6,835 B) + append (162 B) = 6,997 B final, plus
the 6 unchanged prior rows. **Third byte-perfect prediction match
across the Cursor chain** — end-to-end consistency between tool
acks and SQLite persistence is solid.

Full file: [`db-snapshots/after-S3.tsv`](db-snapshots/after-S3.tsv).

Compare to Claude Desktop final state (`after-S3.tsv` on
claude-desktop): 7 rows / 31,145 B. Same row count; Cursor's chain
is ~45% of Opus's byte volume — consistent style observation across
all three Cursor sessions (more concise notes from the weaker model).

## Still outstanding for this session

- Screenshot of S3 `remember` tool-call render → `screenshots/S3-remember.png`
  (visible in user's capture). Not blocking — DB snapshot is the
  load-bearing proof.

## Discoverability finding update across full chain

| Metric                                  | S1   | S2   | S3   | Trend                                        |
|-----------------------------------------|------|------|------|----------------------------------------------|
| Cursor built-in pre-flight calls        | ~9   | ~5   | ~13  | not monotonic; synthesis re-triggered warmup |
| Schema-file reads (remember.json, etc.) | both | both | both | ✗ persistent every session                   |
| Host-shell `ls /memory` failures        | 0    | 2    | 1    | persistent friction                          |
| `density` usage                         | 0    | 0    | 0    | persistent zero on Cursor                    |
| `cat` of prior `/memory` notes          | n/a  | 0    | 6    | prompt-specific (S2 didn't ask, S3 did)      |
| Self-verification of own writes         | no   | yes  | yes  | learned across chain                         |
| Append-mode `remember` usage            | no   | no   | yes  | new behavior surfaced in S3                  |

**Recommendation:** add a `.cursor/rules` system-prompt snippet to
the project's README + DocsVFS quickstart that says something like:

> When working with documentation:
> - Paths starting with `/docs/` or `/memory/` are DocsVFS MCP virtual
>   mounts, not host filesystem paths.
> - Use the `docs` MCP tool (canonical Unix bash) to read `/docs/...`
>   and list `/memory/...`. Use `remember` to write to `/memory/`.
> - Do not try `read_file` or `run_terminal_cmd` for those paths.

This would shave the ~13-call pre-flight from S3 to ~0 on subsequent
Cursor sessions. Tracked as an adoption-friction follow-up.
