# S2 — Sample stratification

**Date:** TBD
**Client:** Claude Desktop (model: TBD)
**Chat ID / export source:** TBD
**Prerequisite:** S1 has run and `/memory` has at least one note

## Prompt (paste-verbatim)

> A teammate is about to run their first attribution experiment. First
> run `ls /memory` to pick up anything the prior session pinned. Then
> explore `/docs` to find how the working samples are stratified — the
> 500/1k/5k/10k bin sizes and the 576 WebOrganizer topic × format
> breakdown — and the 3 most important design choices behind that
> stratification. Use `remember` to pin each as its own note with a
> `note` field explaining why it matters. Cite filenames.

## Scorecard (fill after run)

| Metric                             | Value |
|------------------------------------|-------|
| Steps used                         |       |
| `docs` calls                       |       |
| `remember` calls                   |       |
| Cited S1 notes? (filenames)        |       |
| Filenames cited from `/docs`       |       |
| Tool-call drift?                   | yes / no |
| Final state                        |       |

## Carry-over evidence (the C2 claim)

<!--
Did the model run `ls /memory` before re-exploring /docs?
Did it `cat` any of S1's notes by name?
Did its final answer reference facts that only existed in /memory?
This section is the heart of the C2 (memory across sessions) evidence.
-->

## Transcript

<!--
Paste full conversation here.
-->
